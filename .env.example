# geminicli-bridge configuration
# Copy this to .env and adjust as needed

# Port for the bridge proxy server (default: 18791)
BRIDGE_PORT=18791

# Gemini CLI model to use
# Available models (use 'gemini --model' to see all):
#   gemini-3-pro-preview     — Gemini 3 Pro Preview (recommended)
#   gemini-3-flash-preview   — Gemini 3 Flash Preview (faster, lower cost)
#   gemini-2.5-pro           — Gemini 2.5 Pro
#   gemini-2.5-flash         — Gemini 2.5 Flash
GEMINI_MODEL=gemini-3-pro-preview

# Path to gemini CLI binary (default: 'gemini' from PATH)
# GEMINI_BIN=/path/to/gemini

# Approval mode for tool execution:
#   yolo     — Auto-approve everything (default for bridge mode)
#   default  — Prompt for approval on each action
#   auto_edit — Auto-approve file edits, prompt for commands
#   plan     — Read-only mode (requires experimental settings in Gemini CLI)
GEMINI_APPROVAL_MODE=yolo

# Working directory for Gemini CLI (affects file access scope)
# GEMINI_WORKING_DIR=/path/to/workspace

# Request timeout in milliseconds (default: 5 minutes)
# BRIDGE_TIMEOUT_MS=300000

# Maximum prompt length in chars before switching to stdin pipe
# BRIDGE_MAX_ARG_LEN=32768

# Token estimation: chars per token (default: 3.5)
# BRIDGE_CHARS_PER_TOKEN=3.5
